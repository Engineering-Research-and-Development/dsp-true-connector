server.port=8086
spring.application.name=dcp-verifier

# Logging
logging.level.it.eng.dcp=DEBUG
logging.level.org.springframework=INFO

# Note: Verifier is a stateless service and does not require MongoDB

