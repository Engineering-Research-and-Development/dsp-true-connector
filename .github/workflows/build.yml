name: Feature or hotfix build

on:
  push:
    branches: [ "feature/*", "hotfix/*", "fix/*" ]

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    env:
      REGISTRY: ghcr.io
      IMAGE_NAME: engineering-research-and-development/dsp-true-connector

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build with Maven
        run: mvn -B clean verify --file pom.xml

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for connector Docker image
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}

      - name: Build and push connector Docker image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: ./connector
          file: ./connector/Dockerfile
          no-cache: true
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:test
          labels: ${{ steps.meta.outputs.labels }}

      - name: Build and push data-plane-http-pull Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./data-plane/data-plane-http-pull
          file: ./data-plane/data-plane-http-pull/Dockerfile
          no-cache: true
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-data-plane-http-pull:test

      - name: Build and push data-plane-http-push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./data-plane/data-plane-http-push
          file: ./data-plane/data-plane-http-push/Dockerfile
          no-cache: true
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-data-plane-http-push:test

  negotiation-api-without-counteroffer-tests:
    needs: build-and-push-image
    runs-on: ubuntu-latest
    
    steps:
      - name: Git Checkout
        uses: actions/checkout@v4

      - name: Run docker container for negotiation-api-without-counteroffer-tests
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env up -d

      - name: Wait for connectors to be healthy
        run: |
          echo "Waiting for connector-a (port 8080) and connector-b (port 8090)..."
          for i in $(seq 1 36); do
            A=$(curl -sf http://localhost:8080/actuator/health -o /dev/null && echo ok || echo fail)
            B=$(curl -sf http://localhost:8090/actuator/health -o /dev/null && echo ok || echo fail)
            if [ "$A" = "ok" ] && [ "$B" = "ok" ]; then
              echo "Both connectors healthy after $((i * 10)) seconds"
              break
            fi
            echo "Attempt $i/36 — connector-a=$A connector-b=$B — waiting 10s..."
            sleep 10
          done
          curl -sf http://localhost:8080/actuator/health || (echo "connector-a unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-a && exit 1)
          curl -sf http://localhost:8090/actuator/health || (echo "connector-b unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-b && exit 1)

      - name: Check if the container is up and running
        run: docker ps -a

      - uses: matt-ball/newman-action@v2.0.0
        with:
          collection: ./ci/docker/test-cases/negotiation-api-without-counteroffer-tests/negotiation-api-without-counteroffer-tests.json

      - name: Dump docker negotiation-api-without-counteroffer-tests
        if: failure()
        uses: jwalton/gh-docker-logs@v2

      - name: Stop docker container negotiation-api-without-counteroffer-tests
        if: always()
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env down -v

  negotiation-api-with-counteroffer-tests:
    needs: build-and-push-image
    runs-on: ubuntu-latest

    steps:
      - name: Git Checkout
        uses: actions/checkout@v4

      - name: Run docker container for negotiation-api-with-counteroffer-tests
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env up -d

      - name: Wait for connectors to be healthy
        run: |
          echo "Waiting for connector-a (port 8080) and connector-b (port 8090)..."
          for i in $(seq 1 36); do
            A=$(curl -sf http://localhost:8080/actuator/health -o /dev/null && echo ok || echo fail)
            B=$(curl -sf http://localhost:8090/actuator/health -o /dev/null && echo ok || echo fail)
            if [ "$A" = "ok" ] && [ "$B" = "ok" ]; then
              echo "Both connectors healthy after $((i * 10)) seconds"
              break
            fi
            echo "Attempt $i/36 — connector-a=$A connector-b=$B — waiting 10s..."
            sleep 10
          done
          curl -sf http://localhost:8080/actuator/health || (echo "connector-a unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-a && exit 1)
          curl -sf http://localhost:8090/actuator/health || (echo "connector-b unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-b && exit 1)

      - name: Check if the container is up and running
        run: docker ps -a

      - uses: matt-ball/newman-action@v2.0.0
        with:
          collection: ./ci/docker/test-cases/negotiation-api-with-counteroffer-tests/negotiation-api-with-counteroffer-tests.json

      - name: Dump docker negotiation-api-with-counteroffer-tests
        if: failure()
        uses: jwalton/gh-docker-logs@v2

      - name: Stop docker container negotiation-api-with-counteroffer-tests
        if: always()
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env down -v

  datatransfer-api-http-pull-tests:
    needs: build-and-push-image
    runs-on: ubuntu-latest

    steps:
      - name: Git Checkout
        uses: actions/checkout@v4

      - name: Run docker container for datatransfer-api-http-pull-tests
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env up -d

      - name: Wait for connectors to be healthy
        run: |
          echo "Waiting for connector-a (port 8080) and connector-b (port 8090)..."
          for i in $(seq 1 36); do
            A=$(curl -sf http://localhost:8080/actuator/health -o /dev/null && echo ok || echo fail)
            B=$(curl -sf http://localhost:8090/actuator/health -o /dev/null && echo ok || echo fail)
            if [ "$A" = "ok" ] && [ "$B" = "ok" ]; then
              echo "Both connectors healthy after $((i * 10)) seconds"
              break
            fi
            echo "Attempt $i/36 — connector-a=$A connector-b=$B — waiting 10s..."
            sleep 10
          done
          curl -sf http://localhost:8080/actuator/health || (echo "connector-a unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-a && exit 1)
          curl -sf http://localhost:8090/actuator/health || (echo "connector-b unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-b && exit 1)

      - name: Wait for data plane containers to be healthy
        run: |
          echo "Waiting for pull DPs (ports 9090 and 9092) to be healthy..."
          for i in $(seq 1 18); do
            PA=$(curl -sf http://localhost:9090/actuator/health -o /dev/null && echo ok || echo fail)
            PB=$(curl -sf http://localhost:9092/actuator/health -o /dev/null && echo ok || echo fail)
            if [ "$PA" = "ok" ] && [ "$PB" = "ok" ]; then
              echo "Both pull DPs healthy after $((i * 10)) seconds"
              break
            fi
            echo "Attempt $i/18 — pull-a=$PA pull-b=$PB — waiting 10s..."
            sleep 10
          done
          curl -sf http://localhost:9090/actuator/health || (echo "data-plane-http-pull-a unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs data-plane-http-pull-a && exit 1)
          curl -sf http://localhost:9092/actuator/health || (echo "data-plane-http-pull-b unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs data-plane-http-pull-b && exit 1)

      - name: Check if the container is up and running
        run: docker ps -a

      - uses: matt-ball/newman-action@v2.0.0
        with:
          collection: ./ci/docker/test-cases/datatransfer-api-http-pull-tests/datatransfer-api-http-pull-tests.json

      - name: Dump docker datatransfer-api-http-pull-tests
        if: failure()
        uses: jwalton/gh-docker-logs@v2

      - name: Stop docker container datatransfer-api-http-pull-tests
        if: always()
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env down -v

  datatransfer-api-http-push-tests:
    needs: build-and-push-image
    runs-on: ubuntu-latest
    
    steps:
      - name: Git Checkout
        uses: actions/checkout@v4

      - name: Run docker container for datatransfer-api-http-push-tests
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env up -d

      - name: Wait for connectors to be healthy
        run: |
          echo "Waiting for connector-a (port 8080) and connector-b (port 8090)..."
          for i in $(seq 1 36); do
            A=$(curl -sf http://localhost:8080/actuator/health -o /dev/null && echo ok || echo fail)
            B=$(curl -sf http://localhost:8090/actuator/health -o /dev/null && echo ok || echo fail)
            if [ "$A" = "ok" ] && [ "$B" = "ok" ]; then
              echo "Both connectors healthy after $((i * 10)) seconds"
              break
            fi
            echo "Attempt $i/36 — connector-a=$A connector-b=$B — waiting 10s..."
            sleep 10
          done
          curl -sf http://localhost:8080/actuator/health || (echo "connector-a unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-a && exit 1)
          curl -sf http://localhost:8090/actuator/health || (echo "connector-b unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-b && exit 1)

      - name: Wait for data plane containers to be healthy
        run: |
          echo "Waiting for push DPs (ports 9091 and 9093) to be healthy..."
          for i in $(seq 1 18); do
            PA=$(curl -sf http://localhost:9093/actuator/health -o /dev/null && echo ok || echo fail)
            PB=$(curl -sf http://localhost:9091/actuator/health -o /dev/null && echo ok || echo fail)
            if [ "$PA" = "ok" ] && [ "$PB" = "ok" ]; then
              echo "Both push DPs healthy after $((i * 10)) seconds"
              break
            fi
            echo "Attempt $i/18 — push-a=$PA push-b=$PB — waiting 10s..."
            sleep 10
          done
          curl -sf http://localhost:9093/actuator/health || (echo "data-plane-http-push-a unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs data-plane-http-push-a && exit 1)
          curl -sf http://localhost:9091/actuator/health || (echo "data-plane-http-push-b unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs data-plane-http-push-b && exit 1)

      - name: Check if the container is up and running
        run: docker ps -a

      - uses: matt-ball/newman-action@v2.0.0
        with:
          collection: ./ci/docker/test-cases/datatransfer-api-http-push-tests/datatransfer-api-http-push-tests.json

      - name: Dump docker datatransfer-api-http-push-tests
        if: failure()
        uses: jwalton/gh-docker-logs@v2

      - name: Stop docker container datatransfer-api-http-push-tests
        if: always()
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env down -v

  api-endpoints-tests:
    needs: build-and-push-image
    runs-on: ubuntu-latest
    
    steps:
      - name: Git Checkout
        uses: actions/checkout@v4

      - name: Run docker container for api-endpoints-tests
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env up -d

      - name: Wait for connectors to be healthy
        run: |
          echo "Waiting for connector-a (port 8080) and connector-b (port 8090)..."
          for i in $(seq 1 36); do
            A=$(curl -sf http://localhost:8080/actuator/health -o /dev/null && echo ok || echo fail)
            B=$(curl -sf http://localhost:8090/actuator/health -o /dev/null && echo ok || echo fail)
            if [ "$A" = "ok" ] && [ "$B" = "ok" ]; then
              echo "Both connectors healthy after $((i * 10)) seconds"
              break
            fi
            echo "Attempt $i/36 — connector-a=$A connector-b=$B — waiting 10s..."
            sleep 10
          done
          curl -sf http://localhost:8080/actuator/health || (echo "connector-a unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-a && exit 1)
          curl -sf http://localhost:8090/actuator/health || (echo "connector-b unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-b && exit 1)

      - name: Check if the container is up and running
        run: docker ps -a

      - name: Run api-endpoints-tests
        uses: matt-ball/newman-action@v2.0.0
        with:
          collection: ./ci/docker/test-cases/api-tests/api-endpoints-tests.json

      - name: Run api-endpoints-tenant-tests
        uses: matt-ball/newman-action@v2.0.0
        with:
          collection: ./ci/docker/test-cases/api-tests-tenant/api-endpoints-tenant-tests.json

      - name: Dump docker api-endpoints-tests
        if: failure()
        uses: jwalton/gh-docker-logs@v2

      - name: Stop docker container api-endpoints-tests
        if: always()
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env down -v


  dataset-api-tests:
    needs: build-and-push-image
    runs-on: ubuntu-latest
    
    steps:
      - name: Git Checkout
        uses: actions/checkout@v4

      - name: Run docker container for dataset-api-tests
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env up -d

      - name: Wait for connectors to be healthy
        run: |
          echo "Waiting for connector-a (port 8080) and connector-b (port 8090)..."
          for i in $(seq 1 36); do
            A=$(curl -sf http://localhost:8080/actuator/health -o /dev/null && echo ok || echo fail)
            B=$(curl -sf http://localhost:8090/actuator/health -o /dev/null && echo ok || echo fail)
            if [ "$A" = "ok" ] && [ "$B" = "ok" ]; then
              echo "Both connectors healthy after $((i * 10)) seconds"
              break
            fi
            echo "Attempt $i/36 — connector-a=$A connector-b=$B — waiting 10s..."
            sleep 10
          done
          curl -sf http://localhost:8080/actuator/health || (echo "connector-a unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-a && exit 1)
          curl -sf http://localhost:8090/actuator/health || (echo "connector-b unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-b && exit 1)

      - name: Check if the container is up and running
        run: docker ps -a

      - uses: matt-ball/newman-action@v2.0.0
        with:
          collection: ./ci/docker/test-cases/dataset-api-tests/dataset-api-tests.json

      - name: Dump docker dataset-api-tests
        if: failure()
        uses: jwalton/gh-docker-logs@v2

      - name: Stop docker container dataset-api-tests
        if: always()
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env down -v

  connector-tests:
    needs: build-and-push-image
    runs-on: ubuntu-latest

    steps:
      - name: Git Checkout
        uses: actions/checkout@v4

      - name: Run docker container for connector-tests
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env up -d

      - name: Wait for connectors to be healthy
        run: |
          echo "Waiting for connector-a (port 8080) and connector-b (port 8090)..."
          for i in $(seq 1 36); do
            A=$(curl -sf http://localhost:8080/actuator/health -o /dev/null && echo ok || echo fail)
            B=$(curl -sf http://localhost:8090/actuator/health -o /dev/null && echo ok || echo fail)
            if [ "$A" = "ok" ] && [ "$B" = "ok" ]; then
              echo "Both connectors healthy after $((i * 10)) seconds"
              break
            fi
            echo "Attempt $i/36 — connector-a=$A connector-b=$B — waiting 10s..."
            sleep 10
          done
          curl -sf http://localhost:8080/actuator/health || (echo "connector-a unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-a && exit 1)
          curl -sf http://localhost:8090/actuator/health || (echo "connector-b unhealthy" && docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env logs connector-b && exit 1)

      - name: Check if the container is up and running
        run: docker ps -a

      - uses: matt-ball/newman-action@v2.0.0
        with:
          collection: ./ci/docker/test-cases/connector-tests/connector-tests.json

      - name: Dump docker connector-tests
        if: failure()
        uses: jwalton/gh-docker-logs@v2

      - name: Stop docker container connector-tests
        if: always()
        run: docker compose -f ./ci/docker/docker-compose.yml --env-file ./ci/docker/.env down -v
