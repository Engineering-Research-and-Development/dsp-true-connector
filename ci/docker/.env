COMPOSE_PROJECT_NAME=trueconnector

#TLS settings
CONNECTOR_A_KEYSTORE_NAME=connector-a.jks
CONNECTOR_A_KEY_PASSWORD=password
CONNECTOR_A_KEYSTORE_PASSWORD=password
CONNECTOR_A_KEYSTORE_ALIAS=connector-a

CONNECTOR_B_KEYSTORE_NAME=connector-b.jks
CONNECTOR_B_KEY_PASSWORD=password
CONNECTOR_B_KEYSTORE_PASSWORD=password
CONNECTOR_B_KEYSTORE_ALIAS=connector-b

#TRUSTSTORE (used also by IDSCP2)
TRUSTSTORE_NAME=truststore.jks
TRUSTSTORE_PASSWORD=password

CONNECTOR_A_CALLBACK_ADDRESS=http://connector-a:8080
CONNECTOR_B_CALLBACK_ADDRESS=http://connector-b:8080

# Data Plane API Keys
DP_PULL_API_KEY=changeme-pull
DP_PUSH_API_KEY=changeme-push
DP_GRPC_API_KEY=changeme-grpc
DP_KAFKA_API_KEY=changeme-kafka

# Data Plane admin secret — must match application.internal.secret on the connectors
DP_ADMIN_SECRET=internal-service-secret-change-in-prod

# External MinIO URL embedded in presigned URLs returned to Newman.
# Newman now runs directly on the host both locally and in GHA, so localhost:9000 is the safe default.
MINIO_EXTERNAL_URL=http://localhost:9000
