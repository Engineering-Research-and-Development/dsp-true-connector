server.port=9098
spring.application.name=data-plane-kafka

# MongoDB
spring.data.mongodb.uri=mongodb://localhost:27017/data-plane-kafka

# Data Plane self-registration
dataplane.id=${DATAPLANE_ID:dp-kafka-default}
dataplane.endpoint=http://localhost:9098
dataplane.control-plane-admin-endpoint=http://localhost:8080
dataplane.auth-type=API_KEY
dataplane.api-key=changeme
dataplane.control-plane-admin-secret=${DATAPLANE_CONTROL_PLANE_ADMIN_SECRET:internal-service-secret-change-in-prod}

# Kafka transport
dataplane.kafka.bootstrap-servers=${DATAPLANE_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
dataplane.kafka.topic-prefix=${DATAPLANE_KAFKA_TOPIC_PREFIX:stream-topic-}
dataplane.kafka.group-id-prefix=${DATAPLANE_KAFKA_GROUP_PREFIX:stream-group-}

# S3 upload mode — credentials and bucket are provided per-request by the Control Plane via metadata
s3.upload-mode=SYNC

# Encryption key for FieldEncryptionService (must match the control plane's key)
application.encryption.key=${APPLICATION_ENCRYPTION_KEY:5m7mlhmu65zsp6x}

# TLS: false = insecure noop client (dev); true = TLS with custom truststore
server.ssl.enabled=false
