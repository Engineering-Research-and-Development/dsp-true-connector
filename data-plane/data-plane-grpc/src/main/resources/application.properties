server.port=9094
spring.application.name=data-plane-grpc

# MongoDB
spring.data.mongodb.uri=mongodb://localhost:27017/data-plane-grpc

# Data Plane self-registration
dataplane.id=${DATAPLANE_ID:dp-grpc-default}
dataplane.endpoint=http://localhost:9094
dataplane.control-plane-admin-endpoint=http://localhost:8080
dataplane.auth-type=API_KEY
dataplane.api-key=changeme
dataplane.control-plane-registration-key=${DATAPLANE_CONTROL_PLANE_REGISTRATION_KEY:dev-only-registration-bootstrap-key-change-me}

# gRPC server host advertised in prepare responses
grpc.server.host=${GRPC_SERVER_HOST:localhost}
grpc.server.port=${GRPC_SERVER_PORT:9095}

# S3 upload mode — credentials and bucket are provided per-request by the Control Plane via metadata
s3.upload-mode=SYNC

# Encryption key for FieldEncryptionService (must match the control plane's key)
application.encryption.key=${APPLICATION_ENCRYPTION_KEY:5m7mlhmu65zsp6x}

# TLS: false = insecure noop client (dev); true = TLS with custom truststore
server.ssl.enabled=false
