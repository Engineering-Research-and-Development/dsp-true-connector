server.port=9091
spring.application.name=data-plane-http-push
spring.data.mongodb.uri=mongodb://localhost:27017/data-plane-push
dataplane.id=${DATAPLANE_ID:dp-http-push-default}
dataplane.endpoint=http://localhost:9091
dataplane.control-plane-admin-endpoint=http://localhost:8090
dataplane.auth-type=API_KEY
dataplane.api-key=changeme
dataplane.control-plane-admin-secret=${DATAPLANE_CONTROL_PLANE_ADMIN_SECRET:internal-service-secret-change-in-prod}

# S3 upload mode — credentials and bucket are provided per-request by the Control Plane via metadata
s3.upload-mode=SYNC

# Encryption key for FieldEncryptionService (must match the control plane's key)
application.encryption.key=${APPLICATION_ENCRYPTION_KEY:5m7mlhmu65zsp6x}

# TLS: false = insecure noop client (dev); true = TLS with custom truststore
server.ssl.enabled=false

## SSL Configuration (required when server.ssl.enabled=true)
## Custom keystore and truststore for outgoing HTTPS requests (HttpClient, OkHttpClient)
#spring.ssl.bundle.jks.connector.key.alias=dp-push
#spring.ssl.bundle.jks.connector.key.password=password
#spring.ssl.bundle.jks.connector.keystore.location=classpath:certs/dp-push.p12
#spring.ssl.bundle.jks.connector.keystore.password=password
#spring.ssl.bundle.jks.connector.keystore.type=PKCS12
#spring.ssl.bundle.jks.connector.truststore.location=classpath:certs/dsp-truststore.p12
#spring.ssl.bundle.jks.connector.truststore.password=password
#spring.ssl.bundle.jks.connector.truststore.type=PKCS12

## Server SSL configuration for incoming HTTPS
#server.ssl.key-alias=dp-push
#server.ssl.key-password=password
#server.ssl.key-store=classpath:certs/dp-push.p12
#server.ssl.key-store-password=password
#server.ssl.key-store-type=PKCS12
